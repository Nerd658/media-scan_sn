# URL de connexion pour la base de données d'authentification SQLite.
# Par défaut, cela créera un fichier 'media_scan_auth.db' dans le répertoire où le backend est lancé.
SQLITE_DATABASE_URL=sqlite+aiosqlite:///media_scan_auth.db

# URL de connexion pour la base de données de données MongoDB.
# Remplacez ceci par votre propre chaîne de connexion MongoDB Atlas.
MONGO_CONNECTION_STRING="mongodb+srv://user:password@cluster-url.mongodb.net/?appName=AppName"

# Clé secrète pour signer les tokens JWT. Doit être une longue chaîne aléatoire.
# Vous pouvez en générer une avec : python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=a_very_secret_key_that_should_be_changed

# Algorithme de signature pour les tokens JWT.
ALGORITHM=HS256

# Durée de validité du token d'accès en minutes.
ACCESS_TOKEN_EXPIRE_MINUTES=30
